---
title: MyButton
description: A customizable my-button component with multiple variants and sizes.
component: true
---

<ComponentPreview name="my-button" />

## Installation

```bash
npx devcn-ui add my-button
```

## Usage

```tsx
import { MyButton } from '@repo/ui/components/my-button';

export default function Example() {
  return <MyButton>Click me</MyButton>;
}
```

## Examples

### Variants

<ComponentPreview name="my-button-variants" />

### Sizes

<ComponentPreview name="my-button-sizes" />

## API Reference

### MyButton

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'default' \| 'secondary'` | `'default'` | The visual style variant |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | The size of the component |
| `className` | `string` | - | Additional CSS classes |

<PoweredBy packages={[
  { name: "React", icon: "logos:react" },
  { name: "Tailwind CSS", icon: "logos:tailwindcss-icon" }
]} />
