{
  "name": "devcn-ui",
  "version": "0.0.1",
  "bin": {
    "devcn-ui": "dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/DeDevsClub/design-registry-starter.git"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "PORT=3422 turbo dev",
    "lint": "ultracite lint",
    "format": "ultracite format",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "bump-deps": "npx npm-check-updates --deep -u -x shiki,fumadocs-* && pnpm install",
    "bump-ui": "npx shadcn@latest add --all --overwrite -c packages/shadcn-ui",
    "clean": "git clean -xdf node_modules"
  },
  "devDependencies": {
    "@auto-it/first-time-contributor": "^11.3.0",
    "@biomejs/biome": "2.1.3",
    "@types/node": "^24.1.0",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2",
    "ultracite": "^5.1.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.14.0",
  "type": "module",
  "dependencies": {
    "sonner": "^2.0.6",
    "tsup": "^8.5.0",
    "zod": "^4.0.14"
  }
}
